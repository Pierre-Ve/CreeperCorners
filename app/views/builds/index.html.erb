<div class="bg-white pt-3 pb-3">
  <div class="container">
    <%= render 'filter', build: @build %>
    <%= link_to "Reset Filter", root_path, class: "btn btn-outline-primary" %>
  </div>
</div>
<div class="container">
  <%= image_tag 'image-index.png', alt: 'icon', class:"w-100" %>
  <h1 class="mt-3 mb-4">Discover the best Minecraft Builds</h1>
</div>

  <div class="d-flex flex-wrap justify-content-between container">
    <% @builds.each do |build| %>
      <%= render build, build: build, lien: build_path(build), name: "View details" %>
    <% end %>
  </div>
</div>
s
